name: 爬蟲 API

on:
  repository_dispatch:
    types: [crawl]

jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - name: 執行爬蟲
        run: |
          curl -X POST https://api.deepseek.com/v1/...
